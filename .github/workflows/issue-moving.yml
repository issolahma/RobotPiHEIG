name: Add new issues to project

# **What it does**:
# **Why we have it**:
# **Who does it impact**:

on:
  issues:
    types:
      - reopened
      - opened

jobs:
  triage_issues:
    if: github.repository == 'github/docs'
    runs-on: ubuntu-latest

    steps:
      - name: Triage to project board
        uses: davidgl17/moverpi@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
        with:
          action-token: ${{ secrets.GITHUB_TOKEN }}
          project-url: 'https://github.com/RobotPiProject/RobotPiHEIG/projects/1'
          column-name: 'To do'